/* eslint-env mocha */
import { isDebugUser } from './isDebugUser'

describe(isDebugUser.name, function () {
  it('returns false if the user is not debug user')
  it('updates the current user values when value is passed')
})
