/* eslint-env mocha */
import { environmentExtensionMixin } from './environmentExtensionMixin'

describe(environmentExtensionMixin.name, function () {
  it('skips if flagged as null or false')
  it('assigns a log to the environment')
  it('assigns a checkDocument function to the environment')
  it('assigns a getDocument function to the environment')
})
